schemaVersion: 1
name: echo
description: Echo each input back as a same-name output.
runs:
  using: bun-module
  main: ./dist/index.mjs
inputs:
  message:
    description: the message to echo
    required: true
outputs:
  echoed:
    description: the same message, echoed
