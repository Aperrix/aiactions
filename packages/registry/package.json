{
  "name": "@aiactions/registry",
  "version": "1.1.0",
  "private": true,
  "description": "Action acquisition and persistence for AIactions — fetch via git sparse-checkout, version resolution (exact / major-range / branch), and lockfile read/write under `.aiactions/lock.json`.",
  "type": "module",
  "exports": {
    ".": "./src/index.ts",
    "./package.json": "./package.json"
  },
  "scripts": {
    "test": "vp test",
    "check": "vp check"
  },
  "dependencies": {
    "@aiactions/git": "workspace:*",
    "@aiactions/paths": "workspace:*",
    "@aiactions/schema": "workspace:*",
    "semver": "^7.8.0",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@types/semver": "^7.7.1"
  }
}
