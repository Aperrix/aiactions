{
  "name": "@aiactions/exec",
  "version": "1.0.0",
  "private": true,
  "description": "Process primitives for AIactions — spawn shell commands and Node-based `uses:` actions, capture stdio, and parse the FD3 line-delimited frame protocol.",
  "type": "module",
  "exports": {
    ".": "./src/index.ts",
    "./package.json": "./package.json"
  },
  "scripts": {
    "test": "vp test",
    "check": "vp check"
  },
  "dependencies": {
    "@aiactions/schema": "workspace:*"
  },
  "devDependencies": {
    "@aiactions/parser": "workspace:*"
  }
}
