{
  "name": "@aiactions/schema",
  "version": "1.0.0",
  "private": true,
  "description": "Source-of-truth zod schemas and domain types for AIactions — workflows, action manifests, refs, registry, env, shell, plus the `AIactionsError` abstract base hierarchy.",
  "type": "module",
  "exports": {
    ".": "./src/index.ts",
    "./package.json": "./package.json"
  },
  "scripts": {
    "test": "vp test",
    "check": "vp check"
  },
  "dependencies": {
    "zod": "^4.4.3"
  }
}
