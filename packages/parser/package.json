{
  "name": "@aiactions/parser",
  "version": "2.0.0",
  "private": true,
  "description": "AIactions YAML → AST parser — converts workflow and action-manifest documents into typed objects, plus semantic validation beyond what zod alone expresses (cycles, dangling needs, refs).",
  "type": "module",
  "exports": {
    ".": "./src/index.ts",
    "./package.json": "./package.json"
  },
  "scripts": {
    "test": "vp test",
    "check": "vp check"
  },
  "dependencies": {
    "@aiactions/schema": "workspace:*",
    "yaml": "^2.8.4",
    "zod": "^4.4.3"
  }
}
