{
  "name": "@aiactions/git",
  "version": "0.1.0",
  "private": true,
  "description": "Single-responsibility git operations wrapper for AIactions — clone, sparse-checkout, ls-remote, rev-parse, and the `GitError` class. All git invocations across the project go through this package.",
  "type": "module",
  "exports": {
    ".": "./src/index.ts",
    "./package.json": "./package.json"
  },
  "scripts": {
    "test": "vp test",
    "check": "vp check"
  }
}
