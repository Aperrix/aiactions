{
  "name": "@aiactions/core",
  "version": "1.0.0",
  "private": true,
  "description": "Stateful orchestrators for AIactions — workflow runner state machine and install pipeline composing the schema, parser, registry, expression, exec, paths, and git bricks.",
  "type": "module",
  "exports": {
    ".": "./src/index.ts",
    "./package.json": "./package.json"
  },
  "scripts": {
    "test": "vp test",
    "check": "vp check"
  },
  "dependencies": {
    "@aiactions/exec": "workspace:*",
    "@aiactions/expression": "workspace:*",
    "@aiactions/git": "workspace:*",
    "@aiactions/parser": "workspace:*",
    "@aiactions/paths": "workspace:*",
    "@aiactions/registry": "workspace:*",
    "@aiactions/schema": "workspace:*"
  }
}
