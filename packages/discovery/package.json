{
  "name": "@aiactions/discovery",
  "version": "1.0.0",
  "private": true,
  "description": "Workflow file discovery for AIactions — walks the project and home roots, parses every `.yml`/`.yaml` it finds, and surfaces per-file errors without aborting the scan.",
  "type": "module",
  "exports": {
    ".": "./src/index.ts",
    "./package.json": "./package.json"
  },
  "scripts": {
    "test": "vp test",
    "check": "vp check"
  },
  "dependencies": {
    "@aiactions/parser": "workspace:*",
    "@aiactions/schema": "workspace:*"
  }
}
