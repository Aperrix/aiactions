{
  "$schema": "https://json.schemastore.org/claude-code-settings.json",
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "if": "Bash(git commit*)",
            "command": "cmd=$(jq -r '.tool_input.command // \"\"'); case \"$cmd\" in *--amend*|*commit-tree*) exit 0 ;; *\"git commit\"*) ;; *) exit 0 ;; esac; jq -n '{\"hookSpecificOutput\":{\"hookEventName\":\"PostToolUse\",\"additionalContext\":\"Git commit détecté sur AIactions. Appelle mcp__codebase-memory-mcp__detect_changes avec project=\\\"home-aperrix-Documents-PROJECTS-aiactions\\\" et since=\\\"HEAD~1\\\" pour synchroniser le graphe codebase-memory. Si des dérives structurelles significatives apparaissent, lance ensuite index_repository en mode moderate.\"}}'"
          }
        ]
      }
    ]
  }
}
